/**
 * Função do Cliente que dado os argumentos
 * preenche as variáveis globais
*/
void get_params(char const *argv[]);

/**
 * Loop que verifica age perante os inputs do utilizador
*/
int client(int sock);